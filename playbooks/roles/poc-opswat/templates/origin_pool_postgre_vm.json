{
  "metadata": {
    "name": "postgre-vm",
    "namespace": "{{ extra_volterra_namespace }}",
    "labels": {
      "owner": "{{ extra_owner_email.split('@')[0] }}",
      "project": "cloudbuilder"
    },
    "description": "NGINX workshop, PaaS DB",
    "disable": false
  },
  "spec": {
    "origin_servers": [
      {
        "private_ip": {
          "ip": "10.201.{{ extra_volterra_site_tooling_id }}.10",
          "site_locator": {
            "site": {
              "tenant": "{{ extra_volterra.tenant.full }}",
              "namespace": "system",
              "name": "cloudbuilder-{{ extra_volterra_site_tooling_id }}"
            }
          },
          "inside_network": {}
        },
        "labels": {}
      }
    ],
    "no_tls": {},
    "port": 5432,
    "same_as_endpoint_port": {},
    "healthcheck": [],
    "loadbalancer_algorithm": "LB_OVERRIDE",
    "endpoint_selection": "LOCAL_PREFERRED"
  }
}